package com.company.chapter03operators.examples.svodkaoperatorov;

class Scratch_1 {
  /*  void f(boolean b) {}
        void boolTest(boolean x, boolean y) {
// Арифметические операции:
//! x = x * у;
//! x = x / у;
//! x = x % yj
//! x = x + у;
//! x = x - у;
//! x++j
//! x ~ ;
//! x = +y;
//l x = -у;
// Операции сравнения и логические операции:
//! f(x > у);
//! f(x >= y)j
//! f(x < у);
//! f(x <= y)j
            f(x == y);
            f(x != y);
            f ( ! y );
                    x = x && y;
            x = x || y;
// Поразрядные операторы:
//l x = ~y;
            x = x & y;
            x = x | y;
            x = x || y;
//! x = x << 1 ;
//! x = x » 1 ;
//! x = x > » lj
// Совмещенное присваивание:
// 1 X += y;
//! x -= у;
//l x *« yj
//! x /= у;
//! x %= у;
//! x « = 1 ;
//! x » = 1 ;
//! x » > = 1;
            x &= y;
            x ^= y;
            x |= y;
// Приведение:
//! char с = (char)x;
//! byte В = (byte)x;
//! short s = (short)x;
//! int i = (int)x;
//! long 1 = (long)xj
//! float f = (float)x;
//! double d = (double)x;
        }

    void charTest(char x, char y) {
// Арифметические операции:
        x = (char) (x * y);
                x = (char) (x / y);
        x = (char) (x % y) ;
                x = (char) (x + y) ;
                x = (char) (x - y) ;
        x++ ;
        x --;
        x = (char)+y;
        x = (char)-y;
// Операции с р а в н е н и я и л о г и ч е с к и е операции:
        f(x > y);
        f(x >= y);
        f(x < y);
        f(x <= y);
        f(x == y);
        f(x != y);
//!f(!x);
//1 f ( x && у ) ;
//!f(x || y);
// Поразрядные о п е р а ц и и :
        x = (char)~y;
        x = (char)(x & y);
        x = (char )(x | y);
        x = (char )(x %= y );
        x = (char )(x << 1);
        x = (char )(x >> 1);
        x = (char )(x >>> 1);
// Совмещенное п р и с в а и в а н и е :
        x += y;
        x -= y;
        x *= y;
        x /= y;
        x %= y;
        x <<= 1;
        x >>= 1;
        x >>>= 1;
        x &= y;
        x ^= y;
        x |= y;
// Пр ив еден ие :
//!b o o l e a n b = (b o o l e a n )x;
        byte В = (byte) x;
        short s = (short) x;
        int i = (int) x;
        long l = (long) x;
        float f = (float) x;
        double d = (double) x;
    }

    void byteTest ( byte x , byte y ) {
// А рифмет ич ес кие о п е р а ц и и :
        x=(byte)(x*y);
                x=(byte)(x/y);
                x=(byte)(x%y);
        x=(byte)(x+y);
                x=(byte)(x-y);
        x++;
        //x;
                x=(byte)+y;
        x=(byte)-y;
// Операции с р а в н е н и я и л о г и ч е с к и е опе рации
        f(x > y) ;
        f(x >= y);
        f(x < y);
        f(x <= y);
        f(x == y);
        f(x != y);
//! f(!x)j
//! f(x && у);
//! f(x M у);
// Поразрядные операции:
        x = (byte) ~y;
        x = (byte) (x & y);
        x = (byte) (x | y);
        x = (byte) (x ^ y) ;
                x = (byte) (x << 1);
        x = (byte) (x >> 1 );
        x = (byte) (x >>> 1);
// Совмещенное присваивание:
        x += y;
        x -= y;
        x *= y;
        x /= y;
        x %= y;
        x <<= 1;
        x >>= 1;
        x >>>= 1;
        x &= y;
        x ^= y;
        x |= y;
// Приведение:
//! boolean b = (boolean)x;
        char с = (char)x;
        short s = (short)x;
        int i = (int)x;
        long l = (long)x;
        float f = (float)x;
        double d = (double)x;
    }

    void shortTest(short x, short у) {
// Арифметические операции:
        x = (short) (x * у);
        x = (short) (x / у);
        x = (short) (x % у);
        x = (short) (x + у);
        x = (short) (x - у);
        x++;
        x--;
        x = (short) +y;
        x = (short) -y;
// Операции сравнения и логические операции:
        f(x > y) j
        f(x >= у);
        f(x < у);
        f(x <= у);
        f(x == у) j
        f(x != у);
//! f(!x);
//! f(x && у);
//! f(x M у);
// Поразрядные операции:
        x = (short) ~y;
        x = (short) (x & у);
        x = (short) (x | у);
        x = (short) (x Л у);
        x = (short) (x << 1);
        x = (short) (x >> 1);
        x = (short) (x >>> l) j
// Совмещенное присваивание:
        x += у;
        x -= у;
        x *= у;
        x /= у;
        x %= у;
        x <<= 1;
        x >>= 1;
        x >>>= 1;
        x &= yj
        x ^= у;
        x |= у;
// Преобразование:
//! boolean b = (boolean)x;
        char с = (char) x;
        byte В = (byte) x;
        int i = (int) x;
        long 1 = (long) xj
        float f = (float) x;
        double d = (double) x;
    }
    void intTest(int x, int у) {
// Арифметические операции:
        x = x * у;
        x = x / у;
        x = x % у;
        x = x + у;
        x = x - у;
        x++j
        x--;
        x = +y;
        x = -у;
// Операции сравнения и логические операции
        f(x > у);
        f(x >= у);
        f(x < y)j
        f(x <= у);
        f<x == у);
        f(x != у);
/ / 1 f(!x);
/ / \ f(x && у);
//» f(* If Y)J
// Поразрядные операции:
        x = ~y;
        x = x & у;
        x = x | у;
        x = x ^ у;
        x = x << lj
        x = x » 1;
        x = x > » 1 ;
// Совмещенное присваивание:
        x += у;
        x -= у;
        x *= у;
        x /= у;
        x %= у;
        x « = 1 ;
        x >>= 1 ;
        x >>>= 1 ;
        x &= у;
        x ^= у;
        x 1= у;
// Приведение:
//! boolean b = (boolean)x;
        char с = (char)x;
        byte В = (byte)x;
        short s = (short)x;
        long 1 = (long)xj
        float f = (float)x;
        double d = (double)x;
>
        void longTest(long x, long у) {
// Арифметические операции:
            x = x * у;
            x = x / у;
            x = x %у;
            x = x + у;
            x = x - у;
            x++j
            X - - J
            X = +y;
            x = -yj
// Операции сравнения и логические операции:
            f(x > у);
            f(x >= у);
            f(x < у);
            f(x <= у);
            f(x == у);
            f(x != у);
//! f(!x);
//! f(x && y)j
//! f(x M у);
// Поразрядные операции:
            x = ~y;
            x = x & yj
            x = x | у;
            x = x ^ у;
            x = x << 1 ;
            x = x >> 1 ;
            x = x >>> 1 ;
// Совмещенное присваивание:
            x += у;
            x -= у;
            x *= у;
            x /= у;
            x %= у;
            x « = 1 ;
            x >>= 1 ;
            x > » = 1 ;
            x &= у;
            x ^= у;
            x 1= у;
// Приведение:
//! boolean b = (boolean)x;
            char с = (char)xj
            byte В = (byte)xj
            short s = (short)x;
            int i = (int)x;
            float f = (float)xj
            double d = (double)x;
        }
        void floatTest(float x, float у) {
// Арифметические операции:
            x = x * yj
            x = x / yj
            x = x % yj
            x « x + y j
                    x = x - у j
            x++;
            x--;
            x = +yj
            x = -yj
// Операции сравнения и логические операции
            f(x > у);
            f(x >= у);
            f(x < у);
            f(x <= у);
            f(x == у);
            f(x != у);
//i f(!x);
//! f(x && у);
//! f(x M у);
// Поразрядные операции:
//! x = ~yj
//! x = x & у;
//! x = x | у;
//! x = x ^ у;
//! x = x << lj
//! x = x » 1 ;
//! x = x » > 1 ;
// Совмещенное присваивание:
            x += у;
            x -= у;
            x *= у;
            x /= у;
            x %= у;
//! x « = 1 ;
//! x >>= lj
//! x >>>= lj
//! x &= yj
//! x ^= yj
//! x |= yj
// Приведение:
//! boolean b = (boolean)xj
            char с = (char)xj
            byte В = (byte)xj
            short s = (short)xj
            int i = (int)xj
            long 1 = (long)x;
            double d = (double)x;
>
            void doubleTest(double x, double у){
// Арифметические операции:
                x = x * у;
                x = x / у;
                x = x % у;
                x = x + у;
                x = x - у;
                x++ j
                x--;
                x = +y;
                x = -у;
// Операции сравнения и логические операции:
                f(x > у);
                f(x >= у);
                f(x < у);
                f(x <= у);
                f(x == у);
                f(x != у);
//! f(!x);
//! f(x && у);
//! f(x || y)j
// Поразрядные операции:
//! x = ~y;
//! x = x & у;
//! x = x | у;
//! x = x Л уj
//! x = x << 1 ;
//! x = x >> 1 ;
//! x = x >>> lj
// Совмещенное присваивание:
                x += у;
                x -= у;
                x *= у;
                x /= у;
                x %= у;
//! x « = lj
//! x >>= 1 ;
//! x >>>= 1 ;
//! x &= у;
//! x ^= у;
//! x |= у;
// Приведение:
//! boolean b = (boolean)xj
                char с = (char) x;
                byte В = (byte) xj
                short s = (short) xj
                int i = (int) x;
                long 1 = (long) x;
                float f = (float) xj
            }*/

    public static void main(String[] args) {

    }
}