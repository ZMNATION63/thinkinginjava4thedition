package com.company.chapter15generictypes.tasks.task25;

/**
 * 25. (2) Создайте два интерфейса и класс, реализующий оба интерфейса. Создайте два
 * обобщенных метода; у одного параметр-тип ограничивается первым интерфейсом,
 * аудругого — вторым интерфейсом. Создайте экземпляр класса, который реализует
 * оба интерфейса, и покажите, что он может использоваться с обоими обобщенными
 * методами.
 */
